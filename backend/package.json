{
  "name": "geniq-backend",
  "version": "1.0.0",
  "description": "Backend for GENIQ interview preparation platform",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node start-prod.js",
    "dev": "nodemon server.js",
    "build": "cd .. && npm install && npm run build && cd backend",
    "build:backend-only": "echo 'No build step required for Node.js backend'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
